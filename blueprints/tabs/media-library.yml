extends: tabs/media

sections:
    images:
        parent: site.find('media-library')
    videos:
        parent: site.find('media-library')
    audio:
        parent: site.find('media-library')
    documents:
        parent: site.find('media-library')
