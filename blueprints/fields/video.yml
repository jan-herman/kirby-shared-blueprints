# Docs: https://getkirby.com/docs/reference/panel/fields/files

label: field.video.label

info: "{< file.infoString >}"

type: files
query: page.files.filterBy('template', 'video')
multiple: false
uploads:
    template: video