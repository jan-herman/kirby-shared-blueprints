title:
    en: Video
    cs: Video

accept:
    extension: mp4

image:
    query: file.thumbnail.toFile
    cover: true

columns:
    - width: 1/2
        fields:
            caption: fields/caption

    - width: 1/2
        fields:
            thumbnail:
                extends: fields/thumbnail
                query: file.parent.images
                translate: false
