label: section.pages.label

type: pages
layout: list
batch: true
search: true
image:
    query: false
    back: var(--jh-item-image-color-bg)
    color: var(--jh-item-image-color)
templatesIgnore:
    - error
    - media-library
    - nested-blocks
    - nested-block
