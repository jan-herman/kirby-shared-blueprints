label: section.pages.label

type: pages
layout: list
search: true
image:
    query: false
    back: var(--color-gray-100)
    color: currentColor
templatesIgnore:
    - error
    - media-library
    - nested-blocks
    - nested-block
